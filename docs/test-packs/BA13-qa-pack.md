# BA13 QA Pack

## Slice metadata

- slice_id: BA13
- app: pace-base2
- requirement_path: docs/requirements/BA13-scanning-runtime-validation-requirements.md

## Manual frontend scenarios

| scenario_ref | route_or_screen | preconditions | test_steps | expected_result | result | notes |
|---|---|---|---|---|---|---|
| S-01 / AC-01 | /scanning | a user with read:page.scanning navigates to /scanning/:scanPointId where the scan point exists and is_active = true, then the runtime surface renders with scan_point.name in the top bar, the event name, the direction badge, and a focused scan Input | 1) Open `/scanning`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | the runtime surface renders with scan_point.name in the top bar, the event name, the direction badge, and a focused scan Input | Pass/Fail | - |
| S-02 / AC-02 | /scanning | a user without read:page.scanning navigates to /scanning/:scanPointId, then AccessDenied is rendered and no scanning content is shown | 1) Open `/scanning`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | AccessDenied is rendered and no scanning content is shown | Pass/Fail | - |
| S-03 / AC-03 | /scanning | a valid scanPointId that resolves to a row with is_active = false | 1) Open `/scanning`. 2) The page loads. 3) Observe the resulting UI/system response. | the Alert variant="destructive" "Scan point inactive" is rendered with AlertDescription "Scan point inactive — this scan point has been deactivated and cannot accept scans." and a "Back to scanning setup" button. No scan Input is rendered | Pass/Fail | - |
| S-04 / AC-04 | /scanning | a scanPointId that resolves to no accessible row | 1) Open `/scanning`. 2) The page loads. 3) Observe the resulting UI/system response. | the Alert variant="destructive" "Scan point not found" is rendered with the correct description and a "Back to scanning setup" button. No scan Input is rendered | Pass/Fail | - |
| S-05 / AC-05 | /scanning | the runtime surface is loaded and the Supabase client is null, then a centred LoadingSpinner is shown; no scan Input is rendered until the client resolves | 1) Open `/scanning`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | a centred LoadingSpinner is shown; no scan Input is rendered until the client resolves | Pass/Fail | - |
| S-06 / AC-06 | /scanning | a card scan with a card_identifier that matches an active core_member_card row and the participant has an approved base_application for the event (site context) | 1) Open `/scanning`. 2) The HID scanner emits the keystrokes. 3) Observe the resulting UI/system response. | the result panel shows Badge variant="solid-main-normal" "Accepted" and the participant name; the result auto-clears after 3 seconds; the Input refocuses | Pass/Fail | - |
| S-07 / AC-07 | /scanning | a card scan with a card_identifier that matches no core_member_card row | 1) Open `/scanning`. 2) The scan is processed. 3) Observe the resulting UI/system response. | the result panel shows Badge variant="solid-sec-muted" "Card not recognised" (card_not_recognised); no Override button is shown; a Dismiss button is shown; the Input is disabled until Dismiss is pressed | Pass/Fail | - |
| S-08 / AC-08 | /scanning | a card scan with a card_identifier that matches a core_member_card row with is_active = false | 1) Open `/scanning`. 2) The scan is processed. 3) Observe the resulting UI/system response. | the result panel shows Badge variant="solid-sec-muted" "Card inactive" (card_not_valid); an Override button is shown (when user has update:page.scanning); a Dismiss button is shown; the Input is disabled until Dismiss is pressed | Pass/Fail | - |
| S-09 / AC-09 | /scanning | a card scan for a known participant with no confirmed booking (activity context) | 1) Open `/scanning`. 2) The scan is processed. 3) Observe the resulting UI/system response. | the result panel shows Badge variant="solid-sec-muted" "Booking not valid" with an Override button (when user has update:page.scanning) and a Dismiss button | Pass/Fail | - |
| S-10 / AC-10 | /scanning | a card scan that produces rejected / duplicate_scan | 1) Open `/scanning`. 2) The result panel renders. 3) Observe the resulting UI/system response. | no Override button is shown (only Dismiss) | Pass/Fail | - |
| S-11 / AC-11 | /scanning | an overridable rejection result and the operator has update:page.scanning | 1) Open `/scanning`. 2) "Override" is pressed. 3) Observe the resulting UI/system response. | the override Dialog opens with DialogTitle "Override scan result", a read-only rejection reason, and a "Notes (optional)" Textarea | Pass/Fail | - |
| S-12 / AC-12 | /scanning | the override Dialog is open | 1) Open `/scanning`. 2) "Confirm override" is pressed. 3) Observe the resulting UI/system response. | a new queue entry with validation_result = 'accepted_override' and override_by = auth.uid() is written to IndexedDB; the dialog closes; the result panel shows Badge variant="solid-acc-normal" "Accepted (override)"; the prior rejected entry is not mutated | Pass/Fail | - |
| S-13 / AC-13 | /scanning | the override Dialog is open | 1) Open `/scanning`. 2) "Cancel" is pressed. 3) Observe the resulting UI/system response. | the dialog closes and the rejected result panel remains visible with the Dismiss and Override buttons intact | Pass/Fail | - |
| S-14 / AC-14 | /scanning | the authenticated user lacks update:page.scanning and a rejection with an overridable class is displayed, then the Override button is absent; only the Dismiss button is shown | 1) Open `/scanning`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | the Override button is absent; only the Dismiss button is shown | Pass/Fail | - |
| S-15 / AC-15 | /scanning | the authenticated user has update:page.scanning | 1) Open `/scanning`. 2) "Manual scan" is pressed. 3) Observe the resulting UI/system response. | the manual scan Dialog opens with a name-search Input and a disabled "Record manual scan" button | Pass/Fail | - |
| S-16 / AC-16 | /scanning | the manual scan Dialog is open and the operator types a name that matches at least one approved base_application row, then a dropdown list of up to 20 matching participants appears below the search Input (matches MS-PA-03 / §7.2) | 1) Open `/scanning`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | a dropdown list of up to 20 matching participants appears below the search Input (matches MS-PA-03 / §7.2) | Pass/Fail | - |
| S-17 / AC-17 | /scanning | a participant is selected in the manual scan Dialog and "Record manual scan" is pressed, then a queue entry with card_identifier = null, validation_result = 'accepted_override', override_by = auth.uid() is written to IndexedDB; the dialog closes; the result panel shows the override-accepted state | 1) Open `/scanning`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | a queue entry with card_identifier = null, validation_result = 'accepted_override', override_by = auth.uid() is written to IndexedDB; the dialog closes; the result panel shows the override-accepted state | Pass/Fail | - |
| S-18 / AC-18 | /scanning | a scan is processed (any outcome) | 1) Open `/scanning`. 2) The queue entry is written to IndexedDB. 3) Observe the resulting UI/system response. | sync_status = 'pending' and the entry is visible in the ba13_scan_queue object store | Pass/Fail | - |
| S-19 / AC-19 | /scanning | the device is offline | 1) Open `/scanning`. 2) A card scan occurs, when the scan is processed and validated against the offline manifest. 3) Observe the resulting UI/system response. | the queue entry is written to IndexedDB with sync_status = 'pending' and the result panel shows the appropriate outcome — identical to the online path | Pass/Fail | - |
| S-20 / AC-20 | /scanning | a duplicate scan (same card_identifier at the same scan point within the dedup window) | 1) Open `/scanning`. 2) The second scan is processed. 3) Observe the resulting UI/system response. | rejected / duplicate_scan is recorded and displayed; no Override button is shown | Pass/Fail | - |
| S-21 / AC-21 | /scanning | the user presses "Back to scanning setup" in the top bar, then navigation proceeds to /scanning | 1) Open `/scanning`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | navigation proceeds to /scanning | Pass/Fail | - |

## Test run summary

- overall result: [Pass | Fail]
- failed scenarios: -
- defect links: N/A
- retest needed: [Yes/No]
