# BA08 QA Pack

## Slice metadata

- slice_id: BA08
- app: pace-base2
- requirement_path: docs/requirements/BA08-units-and-group-coordination-requirements.md

## Manual frontend scenarios

| scenario_id | requirement_ref | route_or_screen | preconditions | test_steps | expected_result | result | notes |
|---|---|---|---|---|---|---|---|
| S-01 | AC-01 | /units | event selected and no units exist, the Units tab DataTable shows "No units have been created for this event." | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given event selected and no units exist, the Units tab DataTable shows "No units have been created for this event." | Pass/Fail | - |
| S-02 | AC-02 | /units | event selected with units, the Units tab shows rows ordered by unit_number ascending with columns Unit #, Unit Name, Subcamp, Contingent, Parent Unit | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given event selected with units, the Units tab shows rows ordered by unit_number ascending with columns Unit #, Unit Name, Subcamp, Contingent, Parent Unit. | Pass/Fail | - |
| S-03 | AC-03 | /units | creating a unit with a blank unit_number, the form shows "Unit number is required." | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given creating a unit with a blank unit_number, the form shows "Unit number is required." | Pass/Fail | - |
| S-04 | AC-04 | /units | creating a unit with unit_number "0", the form shows "Unit number must be a positive integer." | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given creating a unit with unit_number "0", the form shows "Unit number must be a positive integer." | Pass/Fail | - |
| S-05 | AC-05 | /units | creating a unit, the unit appears in the list and a success toast is shown | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given creating a unit, the unit appears in the list and a success toast is shown. | Pass/Fail | - |
| S-06 | AC-06 | /units | editing a unit, the unit_name field accepts blank input and saves as null (not empty string) | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given editing a unit, the unit_name field accepts blank input and saves as null (not empty string). | Pass/Fail | - |
| S-07 | AC-07 | /units | deleting a unit, a ConfirmationDialog with the cascade warning appears; window.confirm is never called | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given deleting a unit, a ConfirmationDialog with the cascade warning appears; window.confirm is never called. | Pass/Fail | - |
| S-08 | AC-08 | /units | confirming delete, the unit is removed and a "Unit deleted" toast appears | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given confirming delete, the unit is removed and a "Unit deleted" toast appears. | Pass/Fail | - |
| S-09 | AC-09 | /units | editing a unit's parent, the unit itself and its descendants do not appear in the parent options | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given editing a unit's parent, the unit itself and its descendants do not appear in the parent options. | Pass/Fail | - |
| S-10 | AC-10 | /units | importing a CSV with valid unit_number rows, units are created and a summary toast shows the count | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given importing a CSV with valid unit_number rows, units are created and a summary toast shows the count. | Pass/Fail | - |
| S-11 | AC-11 | /units | a read-only permission, the create, edit, and delete affordances are not visible | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given a read-only permission, the create, edit, and delete affordances are not visible. | Pass/Fail | - |
| S-12 | AC-12 | /units | creating a role type with a blank title, the form shows "Role title is required." | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given creating a role type with a blank title, the form shows "Role title is required." | Pass/Fail | - |
| S-13 | AC-13 | /units | creating a role type, it appears in the list and a "Role type created" toast appears | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given creating a role type, it appears in the list and a "Role type created" toast appears. | Pass/Fail | - |
| S-14 | AC-14 | /units | deleting a role type, a ConfirmationDialog appears; on confirm, the type is removed | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given deleting a role type, a ConfirmationDialog appears; on confirm, the type is removed. | Pass/Fail | - |
| S-15 | AC-15 | /units | no unit selected, the applicant selector, role type selector, and Assign Role button are not visible | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given no unit selected, the applicant selector, role type selector, and Assign Role button are not visible. | Pass/Fail | - |
| S-16 | AC-16 | /units | selecting a unit, the applicant selector shows only applications with status "approved." | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given selecting a unit, the applicant selector shows only applications with status "approved." | Pass/Fail | - |
| S-17 | AC-17 | /units | all three selectors populated, pressing Assign Role assigns the role and a "Role assigned" toast appears | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given all three selectors populated, pressing Assign Role assigns the role and a "Role assigned" toast appears. | Pass/Fail | - |
| S-18 | AC-18 | /units | the same (unit_id, application_id) pair already has a role, pressing Assign Role updates the role_type_id (upsert) | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given the same (unit_id, application_id) pair already has a role, pressing Assign Role updates the role_type_id (upsert). | Pass/Fail | - |
| S-19 | AC-19 | /units | removing a role, a ConfirmationDialog appears; on confirm, "No role assigned" appears in the row | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given removing a role, a ConfirmationDialog appears; on confirm, "No role assigned" appears in the row. | Pass/Fail | - |
| S-20 | AC-20 | /units | no event selected, the page shows the select-event Card and no data loads | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given no event selected, the page shows the select-event Card and no data loads. | Pass/Fail | - |
| S-21 | AC-21 | /units | an event with no units, the unit selector shows the empty copy | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given an event with no units, the unit selector shows the empty copy. | Pass/Fail | - |
| S-22 | AC-22 | /units | an event with no sessions, the informative Alert appears and no preference form renders | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given an event with no sessions, the informative Alert appears and no preference form renders. | Pass/Fail | - |
| S-23 | AC-23 | /units | selecting a unit with draft preferences, the Available Sessions and Preferences panels render | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given selecting a unit with draft preferences, the Available Sessions and Preferences panels render. | Pass/Fail | - |
| S-24 | AC-24 | /units | adding a session, it moves from Available Sessions to the Preferences list with rank auto-assigned | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given adding a session, it moves from Available Sessions to the Preferences list with rank auto-assigned. | Pass/Fail | - |
| S-25 | AC-25 | /units | removing a preference row, the session returns to Available Sessions and remaining ranks are renumbered contiguously | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given removing a preference row, the session returns to Available Sessions and remaining ranks are renumbered contiguously. | Pass/Fail | - |
| S-26 | AC-26 | /units | manually editing a rank to introduce a gap, the Submit button is disabled and an inline validation message explains the issue | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given manually editing a rank to introduce a gap, the Submit button is disabled and an inline validation message explains the issue. | Pass/Fail | - |
| S-27 | AC-27 | /units | a valid ranked set, pressing Submit Preferences shows the ConfirmationDialog and confirms via app_base_unit_preference_submit | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given a valid ranked set, pressing Submit Preferences shows the ConfirmationDialog and confirms via app_base_unit_preference_submit. | Pass/Fail | - |
| S-28 | AC-28 | /remove/submit | submitted preferences for a unit, the page shows the read-only submitted view with submission timestamp; no add/remove/submit controls are visible | 1) Open `/remove/submit`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given submitted preferences for a unit, the page shows the read-only submitted view with submission timestamp; no add/remove/submit controls are visible. | Pass/Fail | - |
| S-29 | AC-29 | /units | a denied read permission on unit-preferences, AccessDenied renders | 1) Open `/units`. 2) Perform the interaction described by this scenario. 3) Observe the resulting UI/system response. | Given a denied read permission on unit-preferences, AccessDenied renders. | Pass/Fail | - |

## Test run summary

- overall result: [Pass | Fail]
- failed scenarios: -
- defect links: N/A
- retest needed: [Yes/No]
